
#include <Talon/Graphics/Direct3D11GraphicsSupport.h>

namespace Talon
{
	ConstantBuffer* Direct3D11GraphicsSupport::CreateConstantBuffer(size_t bufferSize)
	{
		return nullptr;
	}

	IndexBuffer* Direct3D11GraphicsSupport::CreateIndexBuffer(void* data, size_t indexCount)
	{
		return nullptr;
	}

	VertexBuffer* Direct3D11GraphicsSupport::CreateVertexBuffer(void* data, size_t vertexSize, size_t vertexCount)
	{
		return nullptr;
	}
}